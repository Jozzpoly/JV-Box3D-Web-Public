# JV Web Public — takeover handoff

This repository is public artifact/provenance only. Product development belongs in `Jozzpoly/JV-Box3D-Web-experiment`.

## Fresh entry

1. Resolve live public `main` and private JV-Web `main`.
2. Resolve the GitHub Pages source/status independently when the available tooling permits it.
3. Read `AGENTS.md` and `PUBLICATION_CONTRACT.md`.
4. Read `LIVE_BUILD.json` and `build-manifest.json`.
5. Use the private repository for all product/source work.

Do not reconstruct old release/checkpoint/publication-helper campaigns unless new evidence requires archaeology.

## Current accepted steady-state

- canonical private executable source: `cd7f5f89e8cfb872ff6bddc619e3fb78f2124af4`;
- public executable steady-state promotion: `7efe864a337349f4bbdb9e690c2209a0ee781ba2`;
- Owner-acceptance provenance closure: `086e25c9bd22bddca6462f0d585de6d0fd424012` plus later docs-only descendants;
- previous accepted public steady-state commit: `f512551dc41196bc8ca053357408c93b4b3725be`;
- clean canonical preview artifact used for promotion composition: `fe5ba2c772dbb530848df5bcd55163171b5847bc`;
- approved JSPREV2 is preserved byte-for-byte under the existing provenance rule;
- no executable preview overlay belongs in steady-state public root.

Owner steady-state validation is complete. On Samsung Galaxy A53 / Chrome the Owner confirmed world/vehicle boot, steering, throttle+brake, utility drawer operation, steering-plate default OFF plus ON/OFF toggle, landscape/browser/fullscreen sanity and JSPREV2 loading. `LIVE_BUILD.json` records the compact release-layer acceptance summary; durable product acceptance belongs in the private repository.

## Branch discipline

Only `main` is public authority. The still-visible `preview/p1-2-owner` branch is redundant historical navigation after acceptance, not an active preview or rollback authority. It remains because the current connector session lacks branch-ref deletion; remove it later when ordinary ref-deletion access is available, without creating a special Owner-side cleanup campaign.

A successful public commit proves publication mechanics; Owner/browser/device evidence is required for user-visible acceptance claims.
